# src/lib/

## Purpose

- Shared utilities (HTTP, API, theming, etc.).

## API/HTTP

- Keep fetch/HTTP concerns here rather than scattered through components.
- Prefer typed request/response helpers.
- Keep error mapping consistent and do not leak sensitive details.
