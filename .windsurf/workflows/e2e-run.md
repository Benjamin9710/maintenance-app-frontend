---
description: Run E2E tests headlessly (starts dev server automatically)
---

## Steps

> Required for validating frontend changes.

1. Run Playwright tests headlessly:
   ```
   yarn test:e2e
   ```

// This starts the Vite dev server on port 5173, runs the tests, and stops the server afterward.
